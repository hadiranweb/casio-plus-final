{
  "name": "@casioplus/n8n-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Authenticated orchestration boundary between Casioplus and n8n",
  "main": "dist/server.js",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run src/*.test.ts"
  },
  "dependencies": {
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
